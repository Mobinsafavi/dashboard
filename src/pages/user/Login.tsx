import { Grid, Typography } from "@mui/material";

const Login = () => {
  return (
    <Grid container>
      <Grid item container>
        <Typography>Sign in</Typography>
      </Grid>
    </Grid>
  );
};

export default Login;
